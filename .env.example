# Medusa 4WD Tours - Environment Configuration Example
# Copy this file to .env and replace with your actual values

# CORS Configuration
# Comma-separated list of allowed origins for store, admin, and auth
STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:5173,http://localhost:9000
AUTH_CORS=http://localhost:5173,http://localhost:9000

# Redis Configuration
REDIS_URL=redis://localhost:6379

# CRITICAL: Security Secrets
# NEVER use the example values below in production
# Generate strong secrets using:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# JWT Secret (REQUIRED - minimum 32 characters)
# Used for signing JSON Web Tokens for authentication
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_MINIMUM_32_CHARS

# Cookie Secret (REQUIRED - minimum 32 characters)
# Used for signing cookies to prevent tampering
COOKIE_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_MINIMUM_32_CHARS

# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgres://localhost/medusa-4wd-tours
DB_NAME=medusa-4wd-tours

# Node Environment
# Set to 'production' for production deployments
NODE_ENV=development

# SECURITY NOTES:
# 1. NEVER commit the actual .env file to version control
# 2. Use different secrets for development and production
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Store production secrets in a secure vault (e.g., AWS Secrets Manager)
# 5. Each secret should be cryptographically random and at least 64 characters
