Reset the database and run all migrations.

This command will:
1. Drop the existing database (with confirmation)
2. Create a fresh database
3. Run all migrations
4. Report success

Use this when you need to start with a clean database state.
